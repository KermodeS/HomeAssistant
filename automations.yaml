########  Weather Daily Update  ########
- alias: Daily Weather Forecast
  trigger:
    - platform: time
      at: "08:05:00"
  action:
    - service: shell_command.run_weather_module
    # Add retry after 1 minute if failed
    - delay: "00:01:00"
    - service: shell_command.check_weather_run_log
    - condition: template
      value_template: "{{ is_state('sensor.weather_run_status', 'failed') }}"
    - service: shell_command.run_weather_module

########  Caldaia Shelly Relay Status Updates  ########
- id: "1738425166100"
  alias: Caldaia Shelly Relay - Now ON
  description: ""
  triggers:
    - type: turned_on
      device_id: !secret shelly_caldaia_switch_device_id
      entity_id: !secret shelly_caldaia_switch_entity_id
      domain: switch
      trigger: device
  conditions: []
  actions:
    - service: shell_command.notify_caldaia_on
  mode: single

- id: "1738425278440"
  alias: Caldaia Shelly Relay - Now OFF
  description: ""
  triggers:
    - type: turned_off
      device_id: !secret shelly_caldaia_switch_device_id
      entity_id: !secret shelly_caldaia_switch_entity_id
      domain: switch
      trigger: device
  conditions: []
  actions:
    - service: shell_command.notify_caldaia_off
  mode: single

########  Grocy  ########
- id: grocy_chores_notification
  alias: Grocy Chores Notification
  description: "Send notifications about upcoming chores in Grocy"
  trigger:
    - platform: time
      at: "08:06:00"
    - platform: state
      entity_id: input_boolean.debug_mode
      to: "on"
  action:
    - service: shell_command.run_grocy_module
    # Add retry after 1 minute if failed
    - delay: "00:01:00"
    - service: shell_command.check_grocy_run_log
    - condition: template
      value_template: "{{ is_state('sensor.grocy_run_status', 'failed') }}"
    - service: shell_command.run_grocy_module

########  Grocy Dashboard Update  ########
########  Grocy Dashboard Update  ########
- id: update_grocy_dashboard
  alias: Update Grocy Dashboard Data
  description: "Update the data for the Grocy dashboard"
  trigger:
    # Update every hour
    - platform: time_pattern
      hours: "/1"
    # Also update whenever chores notifications are run
    - platform: event
      event_type: call_service
      event_data:
        domain: shell_command
        service: run_grocy_module
  action:
    - service: shell_command.update_grocy_dashboard

########  Debug Mode Toggle  ########
- id: debug_mode_notification
  alias: Debug Mode Toggle
  description: "Notify when debug mode is toggled"
  trigger:
    - platform: state
      entity_id: input_boolean.debug_mode
  action:
    - service: telegram_bot.send_message
      data:
        message: >-
          Debug mode is now {{ states('input_boolean.debug_mode') }}

########  Voltage Monitoring  ########
# Voltage Alarm Monitoring
- id: "voltage_alarm_monitoring"
  alias: Check Voltage Alarm Levels
  description: "Check Shelly UNI voltage levels and notify if outside thresholds"
  trigger:
    - platform: time_pattern
      seconds: "/5" # Check every 5 seconds (increased slightly from 3 to reduce system load)
    - platform: state
      entity_id: sensor.shellyplusuni_cc7b5c887520_voltmeter
      for: "00:00:03" # 3 second debounce
  action:
    - service: shell_command.check_alarm_voltage # Use the regular command now that we know it works
  mode: single

########  Log Management  ########
- id: log_management
  alias: Log Management
  description: "Cleanup and rotate log files once a day"
  trigger:
    - platform: time
      at: "04:00:00"
  action:
    - service: shell_command.run_log_manager

########  Automation Monitoring  ########
- id: monitor_automations
  alias: Monitor Automations
  description: "Check if automations run successfully, and retry if needed"
  trigger:
    - platform: time_pattern
      hours: "/12"
  action:
    - service: shell_command.check_automation_status
